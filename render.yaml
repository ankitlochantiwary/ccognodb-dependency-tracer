services:
  - type: web
    name: skillgraph
    env: docker
    plan: free
    autoDeploy: false
    envVars:
      - key: COGNODB_URI
        sync: false
      - key: COGNODB_USER
        value: cognodb
      - key: COGNODB_PASSWORD
        sync: false
